## Environment settings for all kind of modes
## Environments used only in interactive shells have to be
## put in ./interactive! An example for this is PS1.

#command -v vim > /dev/null && export EDITOR=/usr/bin/vim 
#if [ -n "$EDITOR" ] ; then  export FCEDIT="${EDITOR}" ; fi

#command -v gpgconf && export SSH_AUTH_SOCK="$(/usr/bin/env gpgconf --list-dirs agent-ssh-socket)"

