#!/usr/bin/bash
#
# ~/.bashrc_local to be included in .bashrc
#

source "${HOME}/.config/bash/env"

[[ $- == *i* ]] && source "${HOME}/.config/bash/interactive"
[[ -e "$HOME/.bashrc_$(hostname -s)" ]] && source "$HOME/.bashrc_$(hostname -s)"
